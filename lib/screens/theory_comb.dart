import 'package:flutter/material.dart';

class TheoryCombPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Theory of Combinations'),
      ),
      body: Center(
        child: Text('This is the Theory of Combinations page'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class TheoryPermPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Theory of Permutations'),
      ),
      body: Center(
        child: Text('This is the Theory of Permutations page'),
      ),
    );
  }
}
